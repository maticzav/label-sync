import { LSConfigurationError } from '../data/labelsync/configuration'

/**
 * Turns a system readable error to a human friendly message.
 * @param error
 */
export function reportError(error: LSConfigurationError): string {
  return ''
}
