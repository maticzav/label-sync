import { common } from '../common'

export const prismaBinding = {
  labels: {
    ...common,
    'area/dataloader': {
      description: 'Dataloader related issues',
      color: '#123456',
    },
  },
}
