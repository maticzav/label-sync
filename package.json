{
  "name": "label-sync",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "pretest": "yarn run build",
    "test": "yarn workspaces run test",
    "precoverage": "yarn run build",
    "coverage": "yarn workspaces run test"
  },
  "devDependencies": {
    "husky": "^1.2.0",
    "prettier": "^1.15.3",
    "pretty-quick": "^1.8.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
