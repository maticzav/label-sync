{
  "name": "label-sync",
  "private": true,
  "scripts": {
    "bootstrap": "lerna exec 'yarn install --network-concurrency 1'",
    "test": "lerna run test",
    "coverage": "lerna run coverage",
    "release": "lerna publish"
  },
  "devDependencies": {
    "create-label-sync": "file:packages/create-label-sync",
    "husky": "^1.2.0",
    "label-sync": "file:packages/label-sync",
    "label-sync-core": "file:packages/label-sync-core",
    "lerna": "^3.5.1",
    "prettier": "^1.15.3",
    "pretty-quick": "^1.8.0"
  }
}
