import { Config } from '.'

const config: Config = {
  'prisma/github-labels': {
    labels: {
      test: {
        description: 'Testing sync.',
        color: '#123456',
      },
    },
  },
}

export default config
